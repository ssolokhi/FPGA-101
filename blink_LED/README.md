# Blink LED

The idea of this project is to design and simulate a device that blinks an LED at a specified frequency.
An input signal **i_enable_LED** and a frequency selector, **i_select_frequency**.
The output is a signal **o_LED_signal**  encoding the LED high/low level.

